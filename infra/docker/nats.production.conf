# NATS production config — token auth guards the public port.
# NATS_AUTH_TOKEN must be set in .env.production (min 32 chars).

authorization {
  token: $NATS_AUTH_TOKEN
}

jetstream {
  store_dir: /data
}

# Expose monitoring on loopback only (mapped to 127.0.0.1:8222 in compose).
http: 8222
