{
  "name": "@gtarp/event-bus",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "clean": "rimraf dist .turbo"
  },
  "dependencies": {
    "@gtarp/event-schema": "workspace:*",
    "nats": "^2.29.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^6.0.3",
    "vitest": "^2.1.5"
  }
}
